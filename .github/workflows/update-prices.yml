name: Update flight prices

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

permissions:
  contents: write

jobs:
  update-prices:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4

      - name: Sync with remote
        run: |
          git pull origin main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Update prices
        run: |
          python scripts/update_prices.py

      - name: Create fallback debug file
        if: always()
        run: |
          mkdir -p artifacts
          echo "Workflow finished at $(date)" > artifacts/workflow_debug.txt
          ls -la > artifacts/root_listing.txt
          ls -la artifacts > artifacts/artifacts_listing.txt || true

      - name: Upload debug files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-files
          path: artifacts/**
          if-no-files-found: warn

      - name: Commit updated JSON
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/prices.json
          git diff --cached --quiet || git commit -m "Update prices.json"

      - name: Rebase and push
        run: |
          git pull --rebase
          git push
